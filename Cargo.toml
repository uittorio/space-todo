[package]
name = "space_todo"
version = "0.1.0"
edition = "2021"

[workspace]
exclude = ["spacetimedb"]

[dependencies]
dotenv = "0.15.0"
reqwest = { version = "0.13.2", features = ["blocking", "json"] }
serde = "1.0.228"
spacetimedb-sdk = { version = "2.1.*" }
